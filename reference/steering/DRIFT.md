# Drift log

Periodic notes from delta scans — what changed vs the last manifest or `CONTEXT.md`.

_Format: see `reference/AGENT.md` § Scan procedures._

## 2026-09-19 — apex /enlistment
`afterclassapp.com/enlistment` is still a 404 (Last-Modified 2026-09-10). Native form is live on `afterclass-website.vercel.app/enlistment`. GitHub push of origin-replica `/enlistment` triggered a blocked preview on Vercel team `afterclass1` (`afterclass-waitlist`), not a production deploy of the apex origin-replica project. This CLI user `sethbplanco-1621` cannot switch into `afterclass1`.

## 2026-09-19 — delta

Manifest still describes a greenfield harness-only repo. Application code has been in-tree for a while: Next.js marketing site, `/enlistment` (legacy `/TestRun` redirects), admin app. Enlistment availability is 2-hour slots at 9am / 11am / 2pm / 4pm (length picker removed).

